<!DOCTYPE html>
<html>
  <head>
    <title>My website</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-dark bg-inverse"
      <div class="container">
        <ul class=class: "nav navbar-nav">
          <li class="nav-item">
            <%= link_to "Home", root_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "about", about_path, class: "nav-link" %>
          </li>
          <% if user_signed_in? %>
            <li class="nav-item pull-md-right">
              <%= link_to "Log out", signout_path, method: :delete, class: "nav-link btn btn-primary" %>
            </li>
          <% else %>
            <li class="nav-item pull-md-right">
              <%= link_to "Sign up", signup_path, class: "nav-link btn btn-primary" %>
            </li>
            <li class="nav-item pull-md-right">
              <%= link_to "Sign in", signin_path, class: "nav-link btn btn-primary" %>
            </li>
          <% end %>
        </ul>
      </div>
    </nav>

    <!-- Flash Messager -->
    <% flash.each do |name, message| %>
      <%= content_tag(:div, message, class: "alert alert-into #{name}") %>
    <% end %>
    
    <%= yield %>
  </body>
</html>
